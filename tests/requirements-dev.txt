# Local test/dev dependencies. boto3 is provided by the Lambda runtime in production (so it is
# NOT in the layer's requirements.txt) but is needed locally to import ddb_helpers under pytest.
-r ../backend/shared/python/requirements.txt
boto3
pytest
# resume_agent renders HTML with Jinja2 (bundled with that function). WeasyPrint is NOT installed
# for tests — rendering.py imports it lazily, so the PDF path is exercised only in deployed smoke.
jinja2>=3.1.6,<4.0
